{
  "name": "chat-box",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "server:dev": "nodemon server.js",
    "server:build": "node server.js",
    "server:test": "jest",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "cy:test": "concurrently \" npm run server:build\" \"npm run client:dev\"",
    "test": "cross-env NODE_ENV=test start-server-and-test cy:test http-get://localhost:3000 cy:open",
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@reduxjs/toolkit": "^1.6.1",
    "bcrypt": "^5.0.1",
    "cross-env": "^7.0.3",
    "express": "^4.17.1",
    "express-jwt": "^6.1.0",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.0.1",
    "mongodb-memory-server": "^8.0.4",
    "mongoose": "^6.0.0-rc2",
    "socket.io": "^4.1.3"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "cypress": "^9.1.0",
    "nodemon": "^2.0.12",
    "socket.io-client": "^4.4.0",
    "start-server-and-test": "^1.14.0"
  }
}
